name: Update Hacker News

on:
  schedule:
    - cron: "30 0,12 * * *"  # At 00:30 and 12:30 UTC (after weather at :00, stocks at :15)
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "scripts/update_hackernews_only.py"
      - "scripts/update_base.py"

concurrency:
  group: readme-push
  cancel-in-progress: false

jobs:
  test:
    if: github.event_name != 'schedule'
    name: Run test suite
    uses: ./.github/workflows/run-tests.yml

  update-hackernews:
    name: Update Hacker News section
    needs: [test]
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch' || success()
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Pull latest changes
        run: git pull --rebase origin main

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Update Hacker News
        run: python -m scripts.update_hackernews_only

      - name: Commit and push changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          if ! git diff --quiet; then
            git add README.md
            git commit -m "chore: update hacker news"
            for i in 1 2 3 4 5; do
              git push && break
              echo "Push attempt $i failed — rebasing on origin/main..."
              git pull --rebase origin main
              sleep $((i * 3))
            done
          fi
